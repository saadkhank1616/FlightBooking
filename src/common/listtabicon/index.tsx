import {StyleSheet, View} from 'react-native';
import React from 'react';
import {ListTabIcon} from '@assets';

export const ListTabicon = () => {
  return (
    <View>
      <ListTabIcon />
    </View>
  );
};

const styles = StyleSheet.create({});
