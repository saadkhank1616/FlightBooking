import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {NotiTabIcon} from '@assets';

export const NotiTabicon = () => {
  return (
    <View>
      <NotiTabIcon />
    </View>
  );
};

const styles = StyleSheet.create({});
