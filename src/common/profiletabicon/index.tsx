import {StyleSheet, View} from 'react-native';
import React from 'react';
import {ProfileTabIcon} from '@assets';

export const ProfileTabicon = () => {
  return (
    <View>
      <ProfileTabIcon />
    </View>
  );
};

const styles = StyleSheet.create({});
